# Dependencies
node_modules/

# Environment / secrets
.env
*.env
!*.env.example

# Firebase / FCM secrets (never commit)
**/google-services.json
**/GoogleService-Info.plist
**/*firebase-adminsdk*.json
app/nova-fcm-service-account.json

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db

# Build output
build/
dist/
.gradle/

# Large local binaries (not for deploy)
cloudflared.exe
server/cloudflared.exe

# Server test uploads (user media; not for repo). Keep the folder via .gitkeep.
server/uploads/*
!server/uploads/.gitkeep
