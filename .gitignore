# Dependencies
node_modules/

# Environment / secrets
.env
*.env
!*.env.example

# Firebase / FCM secrets (never commit)
# NOTE: google-services.json is SAFE to commit (Android API key is public/embedded
# in the APK) and EAS Build needs it checked in, so it is intentionally NOT ignored.
**/GoogleService-Info.plist
**/*firebase-adminsdk*.json
app/nova-fcm-service-account.json

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db

# Build output
build/
dist/
.gradle/

# Large local binaries (not for deploy)
cloudflared.exe
server/cloudflared.exe

# Server test uploads (user media; not for repo). Keep the folder via .gitkeep.
server/uploads/*
!server/uploads/.gitkeep
